import Link from 'next/link';
import { ArrowRight, BarChart3, ChartCandlestick, Layers3 } from 'lucide-react';
import CustomerMenu from '../../../../components/CustomerMenu';
import { mt4Brokers, mt5Brokers, pammBrokers, mamBrokers, singleBrokers } from '../../../../lib/broker-config';

function getList(category:string, platform:string) { if(category==='copy-trading' && platform==='mt4') return mt4Brokers; if(category==='copy-trading' && platform==='mt5') return mt5Brokers; if(category==='copy-trading' && platform==='synthetic') return [singleBrokers.synthetic]; if(category==='copy-trading' && platform==='ctrader') return [singleBrokers.ctrader]; if(category==='pamm') return pammBrokers; if(category==='mam') return mamBrokers; return []; }
export default async function PerformancePlatformPage({params}:{params:Promise<{category:string;platform:string}>}) { const {category,platform}=await params; const brokers=getList(category,platform); const title=platform==='synthetic'?'Deriv Synthetic':platform==='ctrader'?'cTrader':platform.toUpperCase(); if(!brokers.length) return <main className="min-h-screen bg-[#f7f9f8] p-10"><Link href={`/performance-statistics/${category}`}>← Back</Link><h1 className="mt-10 text-3xl font-black">Environment unavailable</h1></main>; return <main className="min-h-screen bg-[#f7f9f8] text-[#101512]"><header className="sticky top-0 z-40 border-b border-[#e4e9e6] bg-white/95"><div className="mx-auto flex min-h-[76px] max-w-[1440px] items-center justify-between px-5 sm:px-8 lg:px-10"><Link href={`/performance-statistics/${category}`} className="font-black text-[#087f3f]">← {category.toUpperCase()}</Link><CustomerMenu/></div></header><section className="border-b border-[#e4e9e6] bg-white"><div className="mx-auto max-w-[1200px] px-5 py-12 sm:px-8 lg:px-10"><span className="text-[10px] font-black tracking-[.2em] text-[#087f3f]">{category.toUpperCase()} / {title}</span><h1 className="mt-3 text-4xl font-black">{title} broker performance</h1><p className="mt-4 max-w-2xl text-[14px] leading-7 text-[#68726e]">Select the exact broker account environment. Each destination is isolated so the performance shown belongs only to that account.</p></div></section><section className="mx-auto max-w-[1200px] px-5 py-10 sm:px-8 lg:px-10"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{brokers.map((broker:any,index:number)=><Link key={broker.slug} href={`/performance-statistics/${category}/${platform}/${broker.slug}`} className="group rounded-2xl border border-[#dfe7e2] bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><span className="text-[9px] font-black tracking-[.18em] text-[#9aa39f]">ENVIRONMENT {String(index+1).padStart(2,'0')}</span><div className="mt-5 grid h-11 w-11 place-items-center rounded-xl bg-[#edf8f1] text-[#087f3f]">{platform==='mt5'?<BarChart3 size={19}/>:platform==='ctrader'?<Layers3 size={19}/>:<ChartCandlestick size={19}/>}</div><h2 className="mt-6 text-xl font-black">{broker.name}</h2><p className="mt-2 text-[12px] leading-6 text-[#68726e]">Open the isolated {category.replace('-',' ')} performance page for this {title} environment.</p><span className="mt-5 inline-flex items-center gap-2 text-[12px] font-extrabold text-[#087f3f]">View performance <ArrowRight size={15}/></span></Link>)}</div></section></main>; }
