import Link from 'next/link';
import { ArrowRight, BarChart3, ChartCandlestick } from 'lucide-react';
import CustomerMenu from '../../../../components/CustomerMenu';
import { mt4Brokers, mt5Brokers, pammBrokers, mamBrokers, singleBrokers } from '../../../../lib/broker-config';
function getList(category:string,platform:string){if(category==='copy-trading'&&platform==='mt4')return mt4Brokers;if(category==='copy-trading'&&platform==='mt5')return mt5Brokers;if(category==='copy-trading'&&platform==='synthetic')return [singleBrokers.synthetic];if(category==='copy-trading'&&platform==='ctrader')return [singleBrokers.ctrader];if(category==='pamm')return pammBrokers;if(category==='mam')return mamBrokers;return[];}
export default async function RiskPlatformPage({params}:{params:Promise<{category:string;platform:string}>}){const {category,platform}=await params;const brokers=getList(category,platform);if(!brokers.length)return <main className="p-10"><Link href={`/risk-analysis/${category}`}>← Back</Link><h1 className="mt-10 text-3xl font-black">Environment unavailable</h1></main>;const label=platform==='synthetic'?'Deriv Synthetic':platform==='ctrader'?'cTrader':platform.toUpperCase();return <main className="min-h-screen bg-[#f7f9f8] text-[#101512]"><header className="sticky top-0 z-40 border-b border-[#e4e9e6] bg-white/95"><div className="mx-auto flex min-h-[76px] max-w-[1440px] items-center justify-between px-5 sm:px-8 lg:px-10"><Link href={`/risk-analysis/${category}`} className="font-black text-[#c93642]">← {category.toUpperCase()}</Link><CustomerMenu/></div></header><section className="border-b border-[#e4e9e6] bg-white"><div className="mx-auto max-w-[1440px] px-5 py-10 sm:px-8 lg:px-10"><div className="flex flex-wrap items-end justify-between gap-5"><div><span className="text-[10px] font-black tracking-[.2em] text-[#c93642]">RISK / {category.toUpperCase()} / {label}</span><h1 className="mt-3 text-4xl font-black">{label} risk environments</h1><p className="mt-4 text-[14px] text-[#68726e]">Select the exact broker/account environment for isolated risk information.</p></div><Link href={`/performance-statistics/${category}/${platform}`} className="inline-flex items-center gap-2 rounded-xl border border-[#dfe7e2] bg-white px-4 py-2.5 text-[11px] font-black text-[#087f3f]"><BarChart3 size={14}/> View {label} performance</Link></div></div></section><section className="mx-auto max-w-[1440px] px-5 py-10 sm:px-8 lg:px-10"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{brokers.map((b:any,i:number)=><div key={b.slug} className="rounded-2xl border border-[#dfe7e2] bg-white p-6 shadow-sm"><span className="text-[9px] font-black tracking-[.18em] text-[#9aa39f]">ENVIRONMENT {String(i+1).padStart(2,'0')}</span><div className="mt-5 grid h-11 w-11 place-items-center rounded-xl bg-[#fff1f2] text-[#c93642]"><ChartCandlestick size={19}/></div><h2 className="mt-6 text-xl font-black">{b.name}</h2><p className="mt-2 text-[12px] leading-6 text-[#68726e]">Open isolated risk analysis for this environment.</p><Link href={`/risk-analysis/${category}/${platform}/${b.slug}`} className="mt-5 inline-flex items-center gap-2 rounded-xl bg-[#c93642] px-4 py-2.5 text-[11px] font-black text-white">View risk <ArrowRight size={14}/></Link><Link href={`/performance-statistics/${category}/${platform}/${b.slug}`} className="mt-2 inline-flex items-center gap-2 text-[11px] font-black text-[#087f3f]"><BarChart3 size={14}/> Performance statistics</Link></div>)}</div></section></main>}
